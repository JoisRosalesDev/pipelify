# Desglose de Tareas Técnicas: Orquestación y Monitoreo de Pipelines ETL en Tiempo Real

**Cambio:** `realtime-pipeline-orchestration`  
**Plataforma:** Pipelify (SaaS DevTool)  
**Estado:** Plan de Ejecución Técnico — Completado  

---

## Matriz General de Tareas

### Fase 0: Infraestructura y Schemas

| ID Tarea | Nombre | Dependencias (IDs) | Descripción Técnica Atómica | Criterios de Aceptación (Checklist) |
| :--- | :--- | :--- | :--- | :--- |
| `TASK-001` | Configuración de Entorno y Manejo de Dependencias Exactas | Ninguna | Crear `package.json` en frontend con versiones fijas sin prefijos (`next@14.2.5`, `react@18.3.1`, `@xyflow/react@12.0.4`, `@prisma/client@5.17.0`, `tailwindcss@3.4.7`, `zod@3.23.8`) y `requirements.txt` en backend con operador `==` (`fastapi==0.111.1`, `uvicorn==0.30.3`, `pydantic==2.8.2`, `sqlalchemy==2.0.31`, `asyncpg==0.29.0`, `celery==5.4.0`, `redis==5.0.7`). Configurar `.env.example` con las 10 variables obligatorias de entorno. | - [x] Archivo `package.json` contiene versiones fijas exactas sin prefijos `^` ni `~`<br>- [x] Archivo `requirements.txt` contiene versiones fijas exactas fijadas con `==`<br>- [x] Archivo `.env.example` incluye las 10 variables de entorno requeridas (`NEXT_PUBLIC_API_URL`, `NEXT_PUBLIC_WS_URL`, `DATABASE_URL`, `DIRECT_URL`, `UPSTASH_REDIS_URL`, `JWT_SECRET_KEY`, `JWT_ALGORITHM`, `CORS_ORIGINS`, `CELERY_CONCURRENCY`, `LOG_LEVEL`) |
| `TASK-002` | Definición e Implementación del Esquema Prisma | TASK-001 | Crear `prisma/schema.prisma` definiendo los modelos `PipelineExecution` y `PipelineNodeExecution`, el enum `ExecutionStatus` (`PENDING`, `RUNNING`, `COMPLETED`, `FAILED`), los mapeos `@map` a nombres snake_case y los índices relacionales (`@@index([status])`, `@@index([executionId, nodeId])`). Generar el cliente de Prisma. | - [x] Modelo `schema.prisma` implementa las entidades `PipelineExecution` y `PipelineNodeExecution`<br>- [x] Enum `ExecutionStatus` incluye `PENDING`, `RUNNING`, `COMPLETED` y `FAILED`<br>- [x] Índices de base de datos definidos en columnas de estado y consulta rápida<br>- [x] Cliente Prisma generado correctamente vía script |
| `TASK-003` | Implementación de Modelos Asíncronos SQLAlchemy 2.0 | TASK-001 | Crear `backend/app/models/execution.py` definiendo `PipelineExecutionModel` y `PipelineNodeExecutionModel` utilizando la API de mapeo `Mapped` de SQLAlchemy 2.0. Mapear los tipos de columna `JSONB`, `PG_UUID`, enum de estado (`SQLEnum(ExecutionStatus, name="execution_status_enum", create_type=False)`) y la relación con eliminación en cascada (`cascade="all, delete-orphan"`). | - [x] `PipelineExecutionModel` y `PipelineNodeExecutionModel` declarados con API `Mapped` de SQLAlchemy 2.0<br>- [x] Tipos PostgreSQL `JSONB` y `PG_UUID` mapeados para snapshot del DAG e historial<br>- [x] Relación de modelo `execution` a `node_executions` con eliminación en cascada configurada<br>- [x] Atributos de fecha (`started_at`, `finished_at`, `created_at`, `updated_at`) con zona horaria habilitada |
| `TASK-004` | Implementación de Esquemas Pydantic v2 | TASK-001 | Crear `backend/app/schemas/pipeline.py` implementando los modelos Pydantic v2: `ReactFlowNodeData`, `ReactFlowNode`, `ReactFlowEdge`, `PipelineDAGPayload`, `ExecutionResponse`, `PipelineNodeExecutionDetail`, `ExecutionDetailResponse` y `WSEventPayload`. Configurar `model_config = ConfigDict(extra="ignore")` e incluir descripciones técnicas con `Field`. | - [x] `PipelineDAGPayload` valida listas de nodos (mínimo 1) y bordes del DAG<br>- [x] `ReactFlowNodeData` incluye `model_config` para ignorar campos no especificados<br>- [x] `WSEventPayload` estructura los eventos WebSocket (`event`, `execution_id`, `node_id`, `status`, `metrics`, `error_message`, `timestamp`) |

### Fase 1: Core Backend y Message Broker

| ID Tarea | Nombre | Dependencias (IDs) | Descripción Técnica Atómica | Criterios de Aceptación (Checklist) |
| :--- | :--- | :--- | :--- | :--- |
| `TASK-005` | Configuración Base de FastAPI, Sesión DB y Middleware CORS | TASK-001, TASK-003 | Implementar `backend/app/main.py`, `backend/app/core/config.py` y `backend/app/db/session.py`. Configurar el motor asíncrono SQLAlchemy (`async_sessionmaker` con `asyncpg`), la lectura de ajustes desde `.env` con Pydantic Settings, y aplicar `CORSMiddleware` restringiendo orígenes dinámicos (`pipelify.vercel.app`, preview deployments y `localhost:3000`) con `allow_credentials=True`. | - [x] `CORSMiddleware` configurado con orígenes `ALLOWED_ORIGINS` y cabeceras personalizadas (`Authorization`, `Content-Type`, `X-Execution-ID`, `X-Request-ID`)<br>- [x] Sesión de base de datos asíncrona expuesta mediante generador de dependencias `get_db`<br>- [x] Configuración `Pydantic Settings` carga variables de entorno de forma tipada |
| `TASK-006` | Integración del Cliente Upstash Redis Asíncrono | TASK-005 | Crear `backend/app/core/redis.py` para administrar la conexión asíncrona a Upstash Redis mediante `redis.asyncio` (`aioredis`). Exponer métodos para encolado, caché y suscripción asíncrona Pub/Sub a canales de ejecución, asegurando el manejo de reconexiones y excepciones de red. | - [x] Conexión `aioredis` instanciada mediante la variable de entorno `UPSTASH_REDIS_URL`<br>- [x] Instancia de Pub/Sub expuesta para suscripciones dinámicas a canales `execution:{id}`<br>- [x] Funciones de utilidad para cierre seguro del cliente Redis en el apagado de la app |
| `TASK-007` | Configuración de Workers Celery y Broker Redis | TASK-006 | Crear `backend/app/workers/celery_app.py` configurando la instancia de Celery utilizando `UPSTASH_REDIS_URL` como broker de mensajes y backend de resultados. Ajustar serializador a JSON y configurar la concurrencia mediante `CELERY_CONCURRENCY`. | - [x] Instancia Celery configurada con broker y backend apuntando a Upstash Redis<br>- [x] Serialización enforced a JSON para tareas y payloads<br>- [x] Parámetro `CELERY_CONCURRENCY` correctamente integrado a la configuración del worker |
| `TASK-008` | Máquina de Estados y Tareas ETL con Resiliencia en Workers | TASK-003, TASK-006, TASK-007 | Crear `backend/app/workers/tasks.py` y `backend/app/services/orchestrator.py` gestionando el ciclo de vida `PENDING -> RUNNING -> COMPLETED/FAILED`. Implementar reintentos con backoff exponencial y Jitter ($t_{wait} = 2^{attempt} + uniform(0, 1)$), circuit breaker (detención si falla >50% en 60s o `max_retries = 3`), actualización atómica en PostgreSQL y publicación de eventos en el canal Redis `execution:{id}`. | - [x] Transición atómica de estados `PENDING` -> `RUNNING` en base de datos<br>- [x] Algoritmo de backoff exponencial con Jitter aplicado en fallos de nodo<br>- [x] Circuit breaker aborta la ejecución y marca estado `FAILED` al superar umbrales de reintento<br>- [x] Tarea publica eventos `EXECUTION_STARTED`, `NODE_UPDATED` y `EXECUTION_FINISHED` en Redis Pub/Sub |

### Fase 2: Telemetría y WebSockets

| ID Tarea | Nombre | Dependencias (IDs) | Descripción Técnica Atómica | Criterios de Aceptación (Checklist) |
| :--- | :--- | :--- | :--- | :--- |
| `TASK-009` | Endpoint REST de Ingesta Asíncrona (`POST /api/v1/executions`) | TASK-004, TASK-005, TASK-008 | Crear `backend/app/api/v1/endpoints/executions.py` con el endpoint `POST /api/v1/executions`. Validar el payload del DAG con Pydantic, registrar la ejecución en PostgreSQL con estado `PENDING`, encolar la tarea en Redis Celery queue y retornar inmediatamente un código HTTP `202 Accepted` con el `execution_id` en menos de 100ms. | - [x] Endpoint retorna `HTTP 202 Accepted` con el payload JSON `{ execution_id, pipeline_id, status: "PENDING", created_at }`<br>- [x] Latencia de respuesta < 100ms garantizada al no esperar la ejecución ETL<br>- [x] Retorna `HTTP 400 Bad Request` si la estructura del DAG es inválida o no pasa validación Pydantic |
| `TASK-010` | Endpoints REST Complementarios (Detalle, Nodos, Cancelación) | TASK-009 | Extender `executions.py` con los endpoints: `GET /api/v1/executions/{execution_id}` para consultar el estado del pipeline, `GET /api/v1/executions/{execution_id}/nodes` para los resultados individuales de cada nodo, y `POST /api/v1/executions/{execution_id}/cancel` para abortar la ejecución activa en Celery. | - [x] `GET /executions/{id}` devuelve el estado consolidado de la ejecución y el desglose de nodos<br>- [x] `POST /executions/{id}/cancel` actualiza el estado a `FAILED` e interrumpe la tarea Celery<br>- [x] Retorno de `HTTP 404 Not Found` ante identificadores de ejecución inexistentes |
| `TASK-011` | Endpoint WebSocket Gateway y Autenticación JWT (`wss://...`) | TASK-006, TASK-009 | Crear `backend/app/api/v1/endpoints/websockets.py` definiendo la ruta WebSocket `@router.websocket("/ws/executions/{execution_id}")`. Intercepta el parámetro de consulta `token`, validar la firma JWT antes de invocar `websocket.accept()`, y rechazar la conexión con código WS `1008 Policy Violation` si el token es inválido o no autorizado. | - [x] Verificación previa de token JWT antes del apretón de manos (`websocket.accept()`)<br>- [x] Rechazo explícito con código de cierre WS `1008 Policy Violation` ante token no válido<br>- [x] Conexión WebSocket establecida exitosamente cuando las credenciales son válidas |
| `TASK-012` | Streaming Redis Pub/Sub, Heartbeat Ping-Pong y Limpieza en WS | TASK-011 | Implementar la lógica asíncrona dentro del endpoint WebSocket para suscribirse al canal Redis `execution:{execution_id}`. Transmitir eventos al cliente, emitir tramas de heartbeat `{"event": "PING"}` cada 20s, procesar tramas `PONG` de retorno, y garantizar la desuscripción `UNSUBSCRIBE` y el cierre de conexiones Redis en el bloque `finally`. | - [x] Retransmisión en tiempo real de los mensajes de Redis Pub/Sub hacia la conexión WebSocket<br>- [x] Servidor emite tramas `PING` cada 20s y valida respuestas `PONG` del cliente<br>- [x] Cierre automático de conexión tras 60 segundos de inactividad de tramas<br>- [x] Bloque `finally` ejecuta `pubsub.unsubscribe()`, `pubsub.close()` y `redis_client.close()` evitando fugas de memoria |

### Fase 3: Foundation Frontend

| ID Tarea | Nombre | Dependencias (IDs) | Descripción Técnica Atómica | Criterios de Aceptación (Checklist) |
| :--- | :--- | :--- | :--- | :--- |
| `TASK-013` | Configuración de Tailwind CSS, Variables CSS y Tematizado Monocromático | TASK-001 | Configurar `tailwind.config.ts` y `src/app/globals.css` implementando la paleta monocromática basada en `zinc`, las animaciones personalizadas (`pulse-glow`, `dash-flow`), las variables CSS de estados ETL (`--status-pending`, `--status-running`, `--status-completed`, `--status-failed`) y los tokens de consola y WebSockets en modos claro y oscuro. | - [x] `tailwind.config.ts` extiende colores de estado ETL y animaciones `dash-flow` y `pulse-glow`<br>- [x] `src/app/globals.css` define variables HSL para modos claro (`:root`) y oscuro (`.dark`)<br>- [x] Estilos globales aplicados sobre los elementos base de la interfaz |
| `TASK-014` | Implementación de Átomos de UI (Atomic Design) | TASK-013 | Crear los componentes atómicos en `src/components/atoms/`: `StatusBadge.tsx` (para `PENDING`, `RUNNING`, `COMPLETED`, `FAILED`), `ExecutionSpinner.tsx`, `ConnectionIndicator.tsx` (estado WS Live, Reconnecting, Disconnected), `ActionButton.tsx`, `IconWrapper.tsx` y `MetricLabel.tsx`. | - [x] `StatusBadge.tsx` renderiza insignias semánticas con iconos de Lucide-React<br>- [x] `ConnectionIndicator.tsx` refleja estados `WS Connected (LIVE)`, `WS Reconnecting` y `WS Disconnected`<br>- [x] Componentes atómicos creados con clases Tailwind puras y soporte para modo oscuro |
| `TASK-015` | Implementación de Moléculas de UI (Atomic Design) | TASK-014 | Crear las moléculas en `src/components/molecules/`: `NodeHeader.tsx` (cabecera de nodo con icono y título), `ExecutionControls.tsx` (barra de botones para ejecutar, pausar y cancelar), `LogViewerRow.tsx` (fila de consola con formato de timestamp y colores por nivel INFO, WARN, ERROR, SUCCESS), `MetricCard.tsx` y `PipelineInfoBanner.tsx`. | - [x] `LogViewerRow.tsx` aplica colores y etiquetas según nivel de log (`INFO`, `WARN`, `ERROR`, `SUCCESS`)<br>- [x] `ExecutionControls.tsx` expone botones de acción primaria y secundaria accesibles<br>- [x] Componentes moleculares construidos de forma modular reutilizando átomos |
| `TASK-016` | Estructura de Páginas y Layouts Base (Next.js 14 App Router) | TASK-013 | Crear la estructura de rutas en `src/app/(dashboard)/pipelines/` y `src/app/(dashboard)/executions/`, junto con `src/app/layout.tsx` y los proveedores contextuales en `src/components/providers/` (`ReactFlowProvider.tsx` y `WebSocketProvider.tsx`). | - [x] Enrutamiento en App Router de Next.js 14 operativo en `/pipelines` y `/executions/[executionId]`<br>- [x] Providers de React Flow y WebSocket envuelven la jerarquía global de la aplicación<br>- [x] Layout raíz configurado con fuentes tipográficas sans/mono y tema base |

### Fase 4: Motor Gráfico

| ID Tarea | Nombre | Dependencias (IDs) | Descripción Técnica Atómica | Criterios de Aceptación (Checklist) |
| :--- | :--- | :--- | :--- | :--- |
| `TASK-017` | Construcción del Nodo ETL Personalizado (`CustomETLNode.tsx`) | TASK-014, TASK-015 | Crear `src/components/organisms/CustomETLNode.tsx` como nodo visual personalizado para React Flow. Aplicar estilos dinámicos de estado (anillo resplandeciente azul en `RUNNING`, borde verde en `COMPLETED`, rojo en `FAILED`, opacidad en `PENDING`), Handles de conexión y métricas de procesamiento en vivo. | - [x] Nodo personalizado registrado correctamente en la propiedad `nodeTypes` de React Flow<br>- [x] Anillo azul resplandeciente dinámico en estado `RUNNING` (`ring-2 ring-blue-500/20`)<br>- [x] Handles de entrada y salida estilizados y funcionales para creación de bordes |
| `TASK-018` | Implementación del Lienzo React Flow (`PipelineCanvas.tsx`) y Paleta | TASK-016, TASK-017 | Crear `src/components/organisms/PipelineCanvas.tsx` y `SidebarPalette.tsx`. Configurar el lienzo interactivo con soporte de arrastrar y soltar (drag-and-drop) de nodos ETL, controles de zoom/pan, bordes con líneas animadas (`dash-flow`) y manejo del estado mediante el hook `useDAGState.ts`. | - [x] Lienzo renderiza DAGs interactivos con nodos personalizados y conexiones animadas<br>- [x] Operación drag-and-drop desde la paleta lateral genera nuevos nodos en el canvas<br>- [x] Hook `useDAGState` gestiona adecuadamente las adiciones, conexiones y selecciones de nodos |
| `TASK-019` | Paneles de Configuración de Nodos y Consola de Logs Organismos | TASK-015, TASK-017 | Crear `src/components/organisms/NodeConfigPanel.tsx` para inspeccionar y editar parámetros del nodo seleccionado y `ExecutionLogsTable.tsx` para la consola de logs con scroll interactivo y auto-scroll al recibir eventos del WebSocket. | - [x] `NodeConfigPanel.tsx` expone los parámetros del nodo seleccionado para su edición<br>- [x] `ExecutionLogsTable.tsx` aplica scrollbars personalizadas (`console-scrollbar`) y desplazamiento automático<br>- [x] Botón de limpiado de logs y filtros visuales por nivel implementados |
| `TASK-020` | Adaptación Mobile First, Controles Táctiles y Bottom Sheet | TASK-018, TASK-019 | Implementar la experiencia móvil en `PipelineCanvas.tsx` y layouts: lienzo al 100% en pantallas `< 768px`, Bottom Sheet táctil deslizable (`fixed inset-x-0 bottom-0 rounded-t-xl`) para inspeccionar el nodo seleccionado con drag handle (`w-12 h-1.5`), Floating Action Bar (FAB) para ejecución rápida, y consola fullscreen. | - [x] Controles interactivos móviles cuentan con área de toque mínima de `44x44px`<br>- [x] Bottom Sheet deslizable abre automáticamente al seleccionar un nodo en vista móvil<br>- [x] Consola de logs se expande a pantalla completa en dispositivos móviles mediante el botón de toggle |

### Fase 5: Ensamblaje y Reactividad

| ID Tarea | Nombre | Dependencias (IDs) | Descripción Técnica Atómica | Criterios de Aceptación (Checklist) |
| :--- | :--- | :--- | :--- | :--- |
| `TASK-021` | Hook Personalizado de WebSocket con Reconexión Automática (`usePipelineWebSocket.ts`) | TASK-012, TASK-016 | Crear `src/hooks/usePipelineWebSocket.ts` gestionando la conexión a `wss://...`, el intercambio de mensajes heartbeat (respuesta `PONG` a tramas `PING`), y el protocolo de reconexión con Backoff Exponencial con Jitter ($t_{espera} = \min(1000 \cdot 2^{intento} + jitter, 30000)$). | - [x] Hook administra el ciclo de vida completo de la conexión WebSocket<br>- [x] Responde automáticamente con tramas `PONG` ante eventos `PING` recibidos del servidor<br>- [x] Ejecuta reconexión automática con backoff exponencial hasta un máximo de 30s ante caídas de red<br>- [x] Desconexión limpia con código WS `1000` ejecutada al desmontar el hook |
| `TASK-022` | Cliente HTTP API REST y Hook de Ejecución (`usePipelineExecution.ts`) | TASK-009, TASK-010, TASK-016 | Crear `src/lib/api-client.ts` y `src/hooks/usePipelineExecution.ts` para conectar el frontend con FastAPI: envío del DAG (`POST /api/v1/executions`), consulta de historial y cancelación manual de ejecuciones en progreso. | - [x] `api-client.ts` adjunta cabeceras de autorización JWT y maneja errores HTTP asíncronos<br>- [x] `usePipelineExecution` expone métodos `dispatchExecution`, `cancelExecution` y estados de carga (`isLoading`, `error`) |
| `TASK-023` | Plantilla de Orquestación y Monitoreo (`ExecutionDetailTemplate.tsx`) | TASK-018, TASK-019, TASK-021, TASK-022 | Integrar los componentes en `src/components/templates/ExecutionDetailTemplate.tsx` y `PipelineOrchestratorTemplate.tsx`, combinando el canvas de React Flow, controles de ejecución, panel de configuración, consola de logs e indicador de conexión WebSocket. | - [x] Plantilla estructura la rejilla de 12 columnas en escritorio y adaptabilidad fluida en móvil<br>- [x] Transición coordinada entre el modo de diseño del DAG y el modo monitoreo de ejecución en tiempo real |
| `TASK-024` | Integración End-to-End Reactiva (Dispatch -> WS -> React Flow & Logs) | TASK-023 | Conectar la reactividad completa: al hacer clic en "Ejecutar Pipeline", se envía el DAG a `POST /api/v1/executions`, se obtiene la respuesta `202 Accepted`, se abre el socket, se escuchan los eventos (`EXECUTION_STARTED`, `NODE_UPDATED`, `EXECUTION_FINISHED`), se actualizan los estados visuales en React Flow y se transmiten los mensajes a la consola de logs. | - [x] Integración E2E verificada: Dispatch REST HTTP 202 -> Conexión WS -> Actualización de nodos en canvas -> Streaming de logs<br>- [x] Nodos en React Flow actualizan dinámicamente sus insignias y bordes a `RUNNING`, `COMPLETED` o `FAILED`<br>- [x] Consola de logs refleja en tiempo real la telemetría enviada desde Celery vía Redis Pub/Sub<br>- [x] Cierre ordenado de la suscripción y conexión WebSocket al finalizar el pipeline |
