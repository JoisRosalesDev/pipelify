sdd:
  version: "1.0"
  mode: openspec
  strict_tdd: false
  project:
    name: pipelify
    stack:
      framework: Next.js 16.3.0
      language: TypeScript 5.x
      runtime: Bun 1.3.14
      styling: Tailwind CSS 4.x
      linting: ESLint 9.x
  testing:
    runner: bun test
    available: false
    reason: "No test framework or test scripts configured in package.json. Bun test runner available at runtime once tests are added."
